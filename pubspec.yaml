name: borsa_takip
description: Borsa takip ve portföy yonetim uygulamasi.
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.1.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  firebase_core: ^2.27.0
  firebase_auth: ^4.17.8
  cloud_firestore: ^4.15.8
  fl_chart: ^0.66.0
  google_fonts: ^6.1.0
  provider: ^6.1.1
  http: ^1.1.0
  intl: ^0.19.0
  url_launcher: ^6.2.4
  flutter_dotenv: ^5.1.0
  google_generative_ai: ^0.4.7

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.2
  flutter_launcher_icons: ^0.13.1
  flutter_native_splash: ^2.4.0

flutter:
  uses-material-design: true
  assets:
    - .env
    - assets/
    - assets/logo.png

flutter_icons:
  android: true
  ios: true
  image_path: assets/logo.png

flutter_native_splash:
  color: "#0D1117"
  image: assets/logo.png
  android: true
  ios: true
